# NoteWeave 开发日志

## 2026-06-02

目标：
建立项目开发规范，并完成后端 MVP 首个可运行切片。

完成：
1. 新增 `docs/项目规范文档.md`，约定长期 goal 模式、git 检查点、uv 环境、开发日志和验证方式。
2. 安装 `python3.14-venv`，在项目内创建 `.tools/uv`，并安装 `uv 0.11.17`。
3. 新增 `.gitignore`，排除 `.venv/`、`.tools/`、`.uv-cache/`、运行数据库、缓存和前端构建产物。
4. 新增 `backend/pyproject.toml`，后端使用 uv 管理依赖。
5. 新增 `backend/src/noteweave/` 后端包，完成 FastAPI + SQLite 轻量后端。
6. 首版接口覆盖注册登录、课程、成员、知识树、笔记、版本、错题、评论、点赞/收藏、补充/纠错、关键词检索、AI 摘要和标签建议。
7. AI 当前是本地规则降级实现，不依赖真实模型 API；后续可替换为远程 OpenAI-compatible client。
8. 新增 `backend/tests/test_mvp_flow.py`，覆盖课程笔记协作核心闭环。

验证：

```bash
cd /root/project/NoteWeave/backend
env UV_CACHE_DIR=/root/project/NoteWeave/.uv-cache ../.tools/uv/bin/uv run --no-sync pytest
```

结果：

```text
1 passed in 1.32s
```

注意：
1. 当前环境中 `uv` 不在系统 PATH，使用项目内 `../.tools/uv/bin/uv`。
2. uv 默认缓存目录 `/root/.cache/uv` 当前不可写，命令需要设置 `UV_CACHE_DIR=/root/project/NoteWeave/.uv-cache`。
3. `uv run pytest` 会尝试同步依赖并联网；依赖已安装时使用 `uv run --no-sync pytest`。
4. 当前 FastAPI/Starlette 在 Python 3.14 环境中同步路由/同步依赖会挂起，因此后端 API 路由和 FastAPI 依赖函数均使用 `async def`。

遗留：
1. 还没有前端应用。
2. 后端暂用标准库 SQLite repository，后续可按架构文档演进到 SQLAlchemy/SQLModel。
3. AI 模块还没有接入远程模型 API。
4. 文件上传、图片附件和富文本编辑器尚未实现。
5. 当前测试只有一条端到端 MVP flow，后续需要拆分更多权限和异常场景测试。

下一步：
1. 提交后端首个开发检查点。
2. 补充根 README 的运行说明。
3. 继续实现前端课程工作台或完善后端文件/附件能力。

## 2026-06-02 前端工作台切片

目标：
补上可演示的课程工作台前端，连接后端 MVP API。

完成：
1. 新增 `frontend/index.html`、`frontend/styles.css`、`frontend/app.js`。
2. 前端覆盖登录/注册、课程创建/加入、知识树浏览和节点创建、笔记创建/发布/点赞/评论/AI 摘要/AI 标签、错题录入和复习状态、关键词检索、补充/纠错建议。
3. 后端新增 `POST /api/courses/join`，支持只用邀请码加入课程。
4. 更新 README，加入前端打开方式和静态服务预览命令。

验证：

```bash
node --check frontend/app.js
python3 -m py_compile backend/src/noteweave/api/app.py backend/tests/test_mvp_flow.py
cd /root/project/NoteWeave/backend
timeout 30s env UV_CACHE_DIR=/root/project/NoteWeave/.uv-cache ../.tools/uv/bin/uv run --no-sync pytest
cd /root/project/NoteWeave/frontend
python3 -m http.server 5173 --bind 127.0.0.1
curl -I http://127.0.0.1:5173/
```

结果：

```text
1 passed in 1.23s
frontend static server: HTTP/1.0 200 OK
```

遗留：
1. 前端仍是无构建静态应用，还没有 Vue/React 组件化。
2. 还没有真实富文本编辑器、图片附件、思维导图。
3. 协作建议列表目前只保留本次会话提交结果，后端还需要补建议查询接口。
4. 还没有浏览器截图自动化测试。

下一步：
1. 提交前端工作台检查点。
2. 补充后端建议查询、附件上传或引入组件化前端。

## 2026-06-02 协作和 AI 接口整理切片

目标：
补齐协作建议的持久查询闭环，并将 AI 功能整理为可配置远程 API、缺省降级的结构。

完成：
1. 新增 `backend/src/noteweave/core/ai.py`，提供 `AIAssistService`。
2. AI 摘要和标签接口现在优先使用 `NOTEWEAVE_MODEL_BASE_URL`、`NOTEWEAVE_MODEL_API_KEY`、`NOTEWEAVE_CHAT_MODEL` 配置的 OpenAI-compatible `/chat/completions` API。
3. 远程模型未配置、关闭或调用失败时，AI 接口自动使用本地规则降级，并在结果中标记 `source: fallback`。
4. 新增 `GET /api/courses/{course_id}/suggestions`，课程成员可查询课程内补充/纠错建议，支持按状态过滤。
5. 前端协作建议列表改为从后端加载，处理建议后自动刷新。
6. `httpx` 从 dev 依赖调整为后端运行依赖，并更新 `backend/uv.lock`。

验证：

```bash
python3 -m py_compile backend/src/noteweave/api/app.py backend/src/noteweave/core/ai.py backend/tests/test_mvp_flow.py
node --check frontend/app.js
cd /root/project/NoteWeave/backend
timeout 30s env UV_CACHE_DIR=/root/project/NoteWeave/.uv-cache ../.tools/uv/bin/uv run --no-sync pytest
```

结果：

```text
1 passed in 1.34s
```

遗留：
1. 远程 AI 目前按 OpenAI-compatible chat completions 调用，尚未针对具体供应商做错误码和限流处理。
2. 还没有前端 AI 配置页面；配置仍通过环境变量完成。
3. 附件上传和真实富文本编辑器仍未实现。

下一步：
1. 提交协作和 AI 整理检查点。
2. 继续做附件/图片能力或前端组件化。

## 2026-06-02 图片附件切片

目标：
补齐笔记和错题所需的轻量图片上传、预览和正文插入能力。

完成：
1. 新增 `attachments` 表，记录课程、可选笔记/错题关联、原始文件名、存储文件名、类型、大小和访问路径。
2. 新增 `POST /api/attachments`，使用 JSON base64 上传图片，避免引入 `python-multipart` 等额外依赖。
3. 新增 `GET /api/files/{stored_name}`，按不可猜测存储名读取图片，返回可直接用于 Markdown 图片引用的 URL。
4. 配置新增 `NOTEWEAVE_FILE_STORAGE_DIR` 和 `NOTEWEAVE_MAX_UPLOAD_BYTES`，默认保存到后端 `uploads/`，默认限制 5 MiB。
5. 前端笔记正文和错题题干新增图片选择、上传、预览，并把返回的 `![name](url)` 插入文本框。
6. 集成测试覆盖图片上传、文件读取、Markdown 写回笔记。

验证：

```bash
python3 -m py_compile backend/src/noteweave/api/app.py backend/src/noteweave/api/schemas.py backend/src/noteweave/core/database.py backend/src/noteweave/core/settings.py backend/tests/test_mvp_flow.py
node --check frontend/app.js
cd /root/project/NoteWeave/backend
timeout 30s env UV_CACHE_DIR=/root/project/NoteWeave/.uv-cache ../.tools/uv/bin/uv run --no-sync pytest -vv -s
```

结果：

```text
1 passed in 1.15s
```

遗留：
1. 目前只支持图片，暂不支持 PDF、音频或任意文件。
2. 附件读取依赖不可猜测文件名，后续如需要更严格的私有附件，可改成带签名 URL 或认证下载接口。
3. 前端仍是 Markdown 文本插入，还没有真正的富文本编辑器内嵌块操作。

下一步：
1. 提交图片附件检查点。
2. 继续做富文本/知识树可视化或前端组件化。

## 2026-06-02 内容删除切片

目标：
补齐笔记和错题管理闭环中的删除能力，满足用户可以删除自己内容、普通成员不能删除他人内容的权限要求。

完成：
1. 新增 `DELETE /api/notes/{note_id}`，笔记作者或课程维护者可删除笔记。
2. 新增 `DELETE /api/mistakes/{mistake_id}`，错题作者或课程维护者可删除错题。
3. 删除内容时同步清理相关评论、点赞、补充/纠错建议、AI 结果和目标附件文件，减少无目标数据残留。
4. 前端笔记列表和错题列表新增删除操作，删除后自动刷新列表和知识树统计。
5. 集成测试覆盖普通成员删除他人内容失败、作者删除临时笔记/错题成功。

验证：

```bash
python3 -m py_compile backend/src/noteweave/api/app.py backend/tests/test_mvp_flow.py
node --check frontend/app.js
cd /root/project/NoteWeave/backend
timeout 30s env UV_CACHE_DIR=/root/project/NoteWeave/.uv-cache ../.tools/uv/bin/uv run --no-sync pytest -vv -s
```

结果：

```text
1 passed in 1.07s
```

遗留：
1. 删除目前是硬删除；如后续需要审计恢复，可增加 `deleted_at` 软删除和 `audit_logs`。
2. 前端删除确认仍使用浏览器原生 confirm，后续组件化时可换成应用内确认对话框。

下一步：
1. 提交内容删除检查点。
2. 继续补课程编辑/成员管理前端、搜索筛选增强或复习视图。

## 2026-06-02 搜索筛选与跳转切片

目标：
补强课程级检索能力，使搜索更接近需求中的按节点、作者、内容类型过滤，并能从结果进入对应内容位置。

完成：
1. `GET /api/search` 新增 `author_id` 参数，支持按作者过滤笔记和错题。
2. 搜索结果新增 `author_id` 和 `author_name`，前端结果列表展示作者信息。
3. 后端对 `source_type` 做显式校验，避免无效类型静默返回空结果。
4. 前端搜索表单新增作者 ID 和“当前节点”过滤。
5. 搜索结果新增“打开位置”操作，可切换到笔记或错题视图，并高亮目标条目。
6. 集成测试覆盖作者过滤、节点过滤、内容类型过滤和无匹配作者场景。

验证：

```bash
python3 -m py_compile backend/src/noteweave/api/app.py backend/tests/test_mvp_flow.py
node --check frontend/app.js
cd /root/project/NoteWeave/backend
timeout 30s env UV_CACHE_DIR=/root/project/NoteWeave/.uv-cache ../.tools/uv/bin/uv run --no-sync pytest -vv -s
```

结果：

```text
1 passed in 1.12s
```

遗留：
1. 搜索仍是数据库行扫描和基础关键词匹配，尚未实现架构文档里的 `search_chunks` 索引表。
2. 作者过滤当前使用作者 ID，后续可以接成员选择器或作者名搜索。
3. 搜索结果跳转先定位到列表条目，还没有进入独立详情页或正文段落锚点。

下一步：
1. 提交搜索增强检查点。
2. 继续实现复习视图、课程成员管理前端或 `search_chunks` 索引化。

## 2026-06-02 课程设置与成员管理切片

目标：
补齐课程维护者编辑课程基础信息、管理课程成员角色的 P1 管理闭环。

完成：
1. 新增 `PATCH /api/courses/{course_id}`，课程维护者可更新课程名称、简介、学期和标签。
2. 课程名称和简介更新后同步课程根节点，并刷新知识树子路径。
3. 成员角色更新接口新增成员存在性校验，避免无效成员静默成功。
4. 前端新增“设置”页，包含课程信息编辑表单和成员角色列表。
5. 前端加载课程时同步加载成员列表，成员角色可在设置页保存。
6. 集成测试覆盖学生无权编辑课程、维护者编辑课程、知识树根节点同步、成员角色更新。

验证：

```bash
python3 -m py_compile backend/src/noteweave/api/app.py backend/src/noteweave/api/schemas.py backend/tests/test_mvp_flow.py
node --check frontend/app.js
cd /root/project/NoteWeave/backend
timeout 30s env UV_CACHE_DIR=/root/project/NoteWeave/.uv-cache ../.tools/uv/bin/uv run --no-sync pytest -vv -s
```

结果：

```text
1 passed in 1.43s
```

遗留：
1. 还没有防止最后一个维护者被降级的保护。
2. 设置页没有做按钮级权限隐藏，学生提交时依赖后端 403。
3. AI 配置仍是环境变量配置，尚未进入管理设置页。

下一步：
1. 提交课程设置检查点。
2. 继续实现复习视图、收藏能力或 AI 配置状态页。

## 2026-06-02 收藏与复习视图切片

目标：
补齐复习支持中的收藏、按章节拉取复习资料、按标签/题型组织复习内容。

完成：
1. 复用 `reactions.favorite`，前端笔记和错题列表新增收藏操作。
2. 新增 `GET /api/courses/{course_id}/review`，聚合课程内可访问的笔记和错题。
3. 复习接口支持 `mode=all|favorites|recent|top`，以及 `node_id`、`tag`、`question_type` 过滤。
4. 复习结果返回节点路径、标签、收藏状态、错题题型和掌握状态。
5. 前端新增“复习”页，可筛选全部资料、我的收藏、最近更新、高赞笔记，并可限定当前知识节点。
6. 收藏和复习接口增加私有内容访问校验，避免课程成员直接收藏他人私有内容。
7. 集成测试覆盖收藏笔记/错题、收藏模式复习、节点过滤和题型过滤。

验证：

```bash
python3 -m py_compile backend/src/noteweave/api/app.py backend/tests/test_mvp_flow.py
node --check frontend/app.js
cd /root/project/NoteWeave/backend
timeout 30s env UV_CACHE_DIR=/root/project/NoteWeave/.uv-cache ../.tools/uv/bin/uv run --no-sync pytest -vv -s
```

结果：

```text
1 passed in 1.15s
```

遗留：
1. 收藏目前只能新增，取消收藏仍需通过反应 ID 删除，前端还没有“取消收藏”按钮。
2. 复习视图没有做分页，接口暂时返回前 50 条。
3. 最近浏览尚未记录，`recent` 当前代表最近更新。

下一步：
1. 提交收藏与复习视图检查点。
2. 继续做取消收藏、最近浏览，或开始补 `search_chunks` 索引表。

## 2026-06-02 收藏状态与取消收藏切片

目标：
补齐收藏闭环，让笔记、错题和复习资料能显示当前用户收藏状态，并支持取消收藏。

完成：
1. 笔记列表、笔记详情、错题列表、复习资料返回 `is_favorite` 和 `favorite_reaction_id`。
2. 前端收藏按钮根据状态显示“收藏/取消收藏”。
3. 前端通过 `DELETE /api/reactions/{reaction_id}` 取消收藏，取消后刷新笔记、错题和复习资料。
4. 复习资料中的收藏按钮也支持同样的切换逻辑。
5. 集成测试覆盖收藏状态字段、取消收藏、取消后收藏复习列表更新。

验证：

```bash
python3 -m py_compile backend/src/noteweave/api/app.py backend/tests/test_mvp_flow.py
node --check frontend/app.js
cd /root/project/NoteWeave/backend
timeout 30s env UV_CACHE_DIR=/root/project/NoteWeave/.uv-cache ../.tools/uv/bin/uv run --no-sync pytest -vv -s
```

结果：

```text
1 passed in 1.34s
```

遗留：
1. 点赞仍只支持新增，前端还没有取消点赞状态按钮。
2. 最近浏览仍未记录。

下一步：
1. 提交收藏状态检查点。
2. 继续做取消点赞、最近浏览，或开始补 `search_chunks` 索引表。

## 2026-06-02 点赞状态与取消点赞切片

目标：
补齐共享笔记点赞互动闭环，让前端能展示当前用户点赞状态并取消点赞。

完成：
1. 笔记列表、详情、更新响应新增 `is_liked` 和 `like_reaction_id`。
2. 前端笔记卡片显示“已点赞/未点赞”状态。
3. 点赞按钮根据状态切换为“点赞/取消点赞”。
4. 前端使用 `DELETE /api/reactions/{reaction_id}` 取消点赞，后端同步回退 `like_count`。
5. 集成测试覆盖点赞状态字段、取消点赞后 `like_count` 回退和 reaction id 清空。

验证：

```bash
python3 -m py_compile backend/src/noteweave/api/app.py backend/tests/test_mvp_flow.py
node --check frontend/app.js
cd /root/project/NoteWeave/backend
timeout 30s env UV_CACHE_DIR=/root/project/NoteWeave/.uv-cache ../.tools/uv/bin/uv run --no-sync pytest -vv -s
```

结果：

```text
1 passed in 1.29s
```

遗留：
1. 最近浏览仍未记录。
2. 共享笔记还没有按点赞数/更新时间/评论数的前端排序控件。

下一步：
1. 提交点赞状态检查点。
2. 继续做共享笔记排序、最近浏览，或 `search_chunks` 索引表。

## 2026-06-02 共享笔记排序切片

目标：
补齐共享笔记按更新时间、点赞数、评论数排序的协作浏览能力。

完成：
1. `GET /api/notes` 新增 `sort=updated|likes|comments` 参数。
2. 后端对非法排序值返回 400，避免无效排序静默生效。
3. 前端协作页共享笔记区新增排序选择器和刷新按钮。
4. 前端共享笔记列表改为使用 `shared_only=true` 的独立请求，不再复用个人笔记列表顺序。
5. 点赞、评论、发布、删除、收藏等操作后同步刷新共享笔记列表。
6. 集成测试覆盖按点赞排序、按评论排序和非法排序。

验证：

```bash
python3 -m py_compile backend/src/noteweave/api/app.py backend/tests/test_mvp_flow.py
node --check frontend/app.js
cd /root/project/NoteWeave/backend
timeout 30s env UV_CACHE_DIR=/root/project/NoteWeave/.uv-cache ../.tools/uv/bin/uv run --no-sync pytest -vv -s
```

结果：

```text
1 passed in 1.57s
```

遗留：
1. 还没有按“相关度”排序共享笔记，相关度目前只在搜索结果里体现。
2. 共享笔记列表还没有分页。

下一步：
1. 提交共享笔记排序检查点。
2. 继续做最近浏览，或开始补 `search_chunks` 索引表。

## 2026-06-02 最近浏览切片

目标：
补齐复习支持中的最近浏览能力，让用户可以从复习页回到最近打开过的笔记和错题。

完成：
1. 新增 `content_views` 表，记录用户对笔记/错题的最近浏览时间。
2. 新增 `POST /api/views`，记录或更新当前用户对目标内容的浏览时间。
3. 记录浏览时复用笔记/错题访问权限校验，避免记录未授权私有内容。
4. `GET /api/courses/{course_id}/review` 新增 `mode=viewed`，仅返回已浏览内容并按浏览时间排序。
5. 复习资料响应新增 `last_viewed_at`。
6. 前端打开搜索/复习结果位置时自动记录浏览，并在复习列表展示浏览日期。
7. 集成测试覆盖笔记/错题浏览记录、最近浏览复习列表和浏览时间字段。

验证：

```bash
python3 -m py_compile backend/src/noteweave/api/app.py backend/src/noteweave/api/schemas.py backend/src/noteweave/core/database.py backend/tests/test_mvp_flow.py
node --check frontend/app.js
cd /root/project/NoteWeave/backend
timeout 30s env UV_CACHE_DIR=/root/project/NoteWeave/.uv-cache ../.tools/uv/bin/uv run --no-sync pytest -vv -s
```

结果：

```text
1 passed in 1.71s
```

遗留：
1. 目前只有通过前端“打开位置”或调用 `POST /api/views` 才会记录浏览；普通列表曝光不算浏览。
2. 最近浏览没有独立分页，仍沿用复习接口前 50 条限制。

下一步：
1. 提交最近浏览检查点。
2. 继续补 `search_chunks` 索引表或富文本/公式渲染体验。

## 2026-06-02 搜索索引切片

目标：
按架构文档补齐轻量 `search_chunks` 索引表，让搜索不再直接扫描原始笔记和错题，并确保保存、更新、AI 采纳后索引同步刷新。

完成：
1. 新增 `search_chunks` 表与课程、来源索引，字段覆盖来源、课程、知识点、标题、正文、摘要、标签、作者、可见性和更新时间。
2. 笔记创建、编辑、发布、删除时同步刷新或清理搜索索引。
3. 错题创建、编辑、掌握状态更新、删除时同步刷新或清理搜索索引。
4. AI 摘要/标签采纳后同步刷新笔记索引中的 `summary` 和 `tags_text`。
5. 知识点路径刷新时同步更新相关索引的 `node_path`。
6. 搜索接口改为查询 `search_chunks`，保留原有权限过滤、作者过滤、节点过滤、标签过滤、相关度评分、摘要片段和节点影响力更新。
7. 搜索接口增加课程级懒回填，兼容已有数据库中尚未建立索引的旧内容。
8. 集成测试覆盖笔记索引创建、AI 采纳后索引刷新、错题索引创建，以及笔记/错题删除后的索引清理。

验证：

```bash
python3 -m py_compile backend/src/noteweave/core/database.py backend/src/noteweave/api/app.py backend/tests/test_mvp_flow.py
node --check frontend/app.js
cd /root/project/NoteWeave/backend
timeout 30s env UV_CACHE_DIR=/root/project/NoteWeave/.uv-cache ../.tools/uv/bin/uv run --no-sync pytest -vv -s
```

结果：

```text
1 passed in 1.74s
```

遗留：
1. `search_chunks` 仍是 SQLite 简单文本匹配，没有启用 FTS5，也没有向量检索；这符合当前“只访问 API 模型、避免重依赖”的约束。
2. 标签仍以换行文本保存，适合当前轻量检索；如果后续标签复杂化，可再补规范化标签索引表。

下一步：
1. 提交搜索索引检查点。
2. 继续做富文本/公式渲染体验，或开始补更细的权限/导出能力。

## 2026-06-02 轻量 Markdown 和公式渲染切片

目标：
补齐笔记和错题展示层对 Markdown、代码块、图片和 LaTeX 公式文本的基础渲染能力，在不引入重前端依赖和构建链的前提下改善编辑闭环。

完成：
1. 新增 `frontend/render.js` 轻量渲染器，支持标题、段落、列表、引用、链接、图片、行内代码、代码块、行内公式 `$...$` 和块级公式 `$$...$$`。
2. 渲染器默认转义 HTML，并限制链接/图片 URL 协议，避免把用户输入直接作为可执行 HTML 注入页面。
3. 笔记列表改为渲染 `content_text` 的 Markdown 内容，并将 AI 摘要作为独立摘要块展示。
4. 错题列表对题目、正确答案、错误原因和解题思路使用同一套 Markdown/公式渲染。
5. 新增 `.markdown-body`、代码块、图片和公式样式，保证图片预览、代码和公式在列表中可读。
6. 新增 `frontend/render.test.js`，覆盖 Markdown、代码、图片、公式渲染和基础 XSS 防护。

验证：

```bash
node --check frontend/render.js
node --check frontend/app.js
node frontend/render.test.js
python3 -m py_compile backend/src/noteweave/core/database.py backend/src/noteweave/api/app.py backend/tests/test_mvp_flow.py
cd /root/project/NoteWeave/backend
timeout 30s env UV_CACHE_DIR=/root/project/NoteWeave/.uv-cache ../.tools/uv/bin/uv run --no-sync pytest -vv -s
```

结果：

```text
frontend render test passed
1 passed in 1.69s
```

遗留：
1. 公式目前是轻量样式化展示，不是完整 TeX 排版引擎；后续如果允许前端依赖，可接 KaTeX/MathJax。
2. Markdown 支持覆盖 MVP 常用语法，没有实现表格、任务列表和 Mermaid 思维导图。

下一步：
1. 提交轻量 Markdown 和公式渲染检查点。
2. 继续做更细的权限/审计日志，或补导出/分页等演示增强能力。

## 2026-06-02 AI 人工确认切片

目标：
补齐 AI 结果“查看、编辑、采纳或放弃”的闭环，避免生成结果自动覆盖笔记内容。

完成：
1. 新增 `AIResultAcceptRequest`，采纳 AI 摘要时可提交人工编辑后的 `summary`。
2. 采纳 AI 标签时可提交人工编辑后的 `tags`，并同步更新笔记标签和 `search_chunks`。
3. 采纳时会把最终采用的结果写回 `ai_results.result`，并标记 `edited_by`，区分人工确认后的内容。
4. 新增 `POST /api/ai/results/{result_id}/reject`，笔记作者或课程维护者可放弃 AI 结果。
5. 前端 AI 摘要/标签操作改为生成后弹窗预览和编辑，取消弹窗会调用放弃接口，不再自动采纳。
6. 集成测试覆盖编辑后采纳摘要、编辑后采纳标签、放弃 AI 结果且不修改原笔记。

验证：

```bash
python3 -m py_compile backend/src/noteweave/api/schemas.py backend/src/noteweave/api/app.py backend/tests/test_mvp_flow.py
node --check frontend/app.js
node --check frontend/render.js
node frontend/render.test.js
cd /root/project/NoteWeave/backend
timeout 30s env UV_CACHE_DIR=/root/project/NoteWeave/.uv-cache ../.tools/uv/bin/uv run --no-sync pytest -vv -s
```

结果：

```text
frontend render test passed
1 passed in 1.69s
```

遗留：
1. 前端仍使用 `prompt` 做轻量编辑确认；后续可替换为更完整的 AI 结果面板。
2. `ai_results` 只有 `accepted_by/accepted_at` 字段，放弃结果暂复用这两个字段记录处理人和处理时间。

下一步：
1. 提交 AI 人工确认检查点。
2. 继续补审计日志、分页/懒加载，或知识树节点搜索/折叠体验。

## 2026-06-02 知识树搜索与折叠切片

目标：
补齐 FR-059 中知识树展开、折叠和节点搜索能力，让课程结构浏览更接近需求文档中的知识树页。

完成：
1. 新增 `frontend/tree.js`，提供知识树节点 ID 收集和按关键词过滤树的轻量工具。
2. 知识树面板新增节点搜索框，以及一键展开、一键折叠按钮。
3. 普通知识树浏览支持逐层展开/折叠，课程根节点默认展开。
4. 搜索时自动展开匹配路径，并高亮直接匹配的节点。
5. 搜索范围覆盖节点标题、路径、类型和说明。
6. 新增 `frontend/tree.test.js`，覆盖节点 ID 收集、祖先保留、子树过滤和无匹配场景。

验证：

```bash
node --check frontend/tree.js
node --check frontend/app.js
node frontend/tree.test.js
node frontend/render.test.js
python3 -m py_compile backend/src/noteweave/api/app.py backend/tests/test_mvp_flow.py
cd /root/project/NoteWeave/backend
timeout 30s env UV_CACHE_DIR=/root/project/NoteWeave/.uv-cache ../.tools/uv/bin/uv run --no-sync pytest -vv -s
```

结果：

```text
frontend tree/render tests passed
1 passed in 1.86s
```

遗留：
1. 知识树搜索目前是前端内存过滤，适合当前课程规模；课程树非常大时可再改为后端查询或虚拟列表。
2. 折叠状态只保存在当前页面状态中，刷新页面后会回到根节点展开。

下一步：
1. 提交知识树搜索与折叠检查点。
2. 继续补审计日志、分页/懒加载，或节点详情中的讨论/标签聚合展示。

## 2026-06-02 审计日志切片

目标：
按架构文档补齐 `audit_logs`，记录发布、采纳、删除、权限变更等关键操作，增强课程协作内容的可追踪性。

完成：
1. 新增 `audit_logs` 表，记录课程、操作者、动作、目标类型、目标 ID、元数据和时间。
2. 新增 `GET /api/courses/{course_id}/audit-logs`，课程维护者或教师可查看课程审计日志，普通学生不可查看。
3. 课程设置更新、成员角色调整会写入审计日志。
4. 知识节点创建、编辑、移动、删除会写入审计日志。
5. 笔记创建、编辑、发布、删除会写入审计日志。
6. 错题创建、编辑、掌握状态更新、删除会写入审计日志。
7. 补充/纠错建议处理、AI 结果采纳和放弃会写入审计日志。
8. 集成测试覆盖学生读取审计日志失败，以及维护者可看到课程更新、发布、删除、AI、建议处理和成员角色变更记录。

验证：

```bash
python3 -m py_compile backend/src/noteweave/core/database.py backend/src/noteweave/api/app.py backend/tests/test_mvp_flow.py
node --check frontend/app.js
node frontend/tree.test.js
node frontend/render.test.js
cd /root/project/NoteWeave/backend
timeout 30s env UV_CACHE_DIR=/root/project/NoteWeave/.uv-cache ../.tools/uv/bin/uv run --no-sync pytest -vv -s
```

结果：

```text
frontend tree/render tests passed
1 passed in 1.79s
```

遗留：
1. 审计日志目前只提供后端 API，还没有接入前端设置页展示。
2. 删除操作仍是硬删除，审计日志只保留目标类型、目标 ID 和少量元数据，不提供完整内容恢复。

下一步：
1. 提交审计日志检查点。
2. 继续补分页/懒加载，或节点详情中的讨论/标签聚合展示。

## 2026-06-02 节点详情聚合切片

目标：
补齐 FR-018 和 FR-060，让用户点击知识树节点后能看到关联笔记、错题、标签、摘要和讨论，而不仅是计数。

完成：
1. `GET /api/tree/nodes/{node_id}/detail` 在返回关联笔记和错题的基础上，新增 `tag_summary` 标签聚合。
2. 节点详情新增 `summaries`，聚合该节点下已采纳的笔记摘要。
3. 节点详情新增 `recent_comments`，聚合该节点关联笔记/错题的最近讨论。
4. 前端选择知识树节点后自动拉取节点详情。
5. 节点详情面板展示标签、摘要、关联内容和最近讨论。
6. 关联内容可直接跳转到对应笔记或错题位置，并复用最近浏览记录。
7. 集成测试覆盖节点详情中的关联笔记、错题、标签、摘要和讨论聚合。

验证：

```bash
python3 -m py_compile backend/src/noteweave/api/app.py backend/tests/test_mvp_flow.py
node --check frontend/app.js
node frontend/tree.test.js
node frontend/render.test.js
cd /root/project/NoteWeave/backend
timeout 30s env UV_CACHE_DIR=/root/project/NoteWeave/.uv-cache ../.tools/uv/bin/uv run --no-sync pytest -vv -s
```

结果：

```text
frontend tree/render tests passed
1 passed in 2.00s
```

遗留：
1. 节点详情聚合只展示当前节点直接挂载内容，尚未递归包含子节点内容。
2. 最近讨论暂取前 5 条，后续可增加独立分页或讨论详情面板。

下一步：
1. 提交节点详情聚合检查点。
2. 继续补分页/懒加载，或把审计日志接入前端设置页展示。

## 2026-06-02 列表分页与加载更多切片

目标：
补齐需求文档中“课程内笔记、错题和评论数量增长后，列表页应支持分页或懒加载”的性能要求，先覆盖笔记、错题和复习资料列表。

完成：
1. `GET /api/notes` 新增 `limit` 和 `offset` 参数，支持笔记列表和共享笔记列表分页。
2. `GET /api/mistakes` 新增 `limit` 和 `offset` 参数，筛选后再分页返回。
3. `GET /api/courses/{course_id}/review` 新增 `limit` 和 `offset` 参数，排序后分页返回。
4. 前端笔记列表、共享笔记、错题列表和复习资料默认按 `PAGE_SIZE=10` 拉取。
5. 前端新增“加载更多”按钮，按当前列表长度作为 offset 继续追加数据。
6. 刷新、筛选和排序会重新从第一页加载，避免旧分页状态污染。
7. 集成测试覆盖笔记、错题和复习接口的 `limit/offset` 行为。

验证：

```bash
python3 -m py_compile backend/src/noteweave/api/app.py backend/tests/test_mvp_flow.py
node --check frontend/app.js
node frontend/tree.test.js
node frontend/render.test.js
cd /root/project/NoteWeave/backend
timeout 30s env UV_CACHE_DIR=/root/project/NoteWeave/.uv-cache ../.tools/uv/bin/uv run --no-sync pytest -vv -s
```

结果：

```text
frontend tree/render tests passed
1 passed in 1.59s
```

遗留：
1. 评论本身还没有独立列表分页接口；当前通过节点详情只展示最近 5 条。
2. 前端仍使用“加载更多”而不是页码式分页，适合当前轻量单页应用。

下一步：
1. 提交列表分页与加载更多检查点。
2. 继续把审计日志接入前端设置页展示，或补导出/评论分页能力。

## 2026-06-02 审计日志前端展示切片

目标：
把已完成的后端审计日志能力接入设置页，让课程维护者能在界面中查看发布、采纳、删除和权限变更记录。

完成：
1. 前端新增 `auditLogs` 状态，并在课程加载、切换课程、课程设置更新和成员角色更新后刷新。
2. 设置页新增“审计日志”面板，展示最近 20 条审计记录。
3. 审计日志展示动作、目标类型、目标 ID、操作者、时间和元数据。
4. 当前用户无权查看审计日志时，前端以空态提示展示，不阻塞课程设置页。
5. 新增 `.full-width` 和审计日志样式，让日志面板占满设置页宽度。

验证：

```bash
node --check frontend/app.js
node frontend/tree.test.js
node frontend/render.test.js
python3 -m py_compile backend/src/noteweave/api/app.py backend/tests/test_mvp_flow.py
cd /root/project/NoteWeave/backend
timeout 30s env UV_CACHE_DIR=/root/project/NoteWeave/.uv-cache ../.tools/uv/bin/uv run --no-sync pytest -vv -s
```

结果：

```text
frontend tree/render tests passed
1 passed in 1.55s
```

遗留：
1. 审计日志前端当前只读最近 20 条，还没有分页。
2. 操作者暂以用户 ID 展示，后续可在后端 join 用户显示名。

下一步：
1. 提交审计日志前端展示检查点。
2. 继续补导出、评论分页，或更完整的 AI 配置状态页。

## 2026-06-02 个人资料编辑切片

目标：
补齐 FR-002，让用户能够查看并修改个人基础信息中的昵称。

完成：
1. 新增 `UserUpdateRequest`，约束昵称长度和非空。
2. 新增 `PATCH /api/auth/me`，当前登录用户可更新自己的 `display_name`。
3. 前端账号栏新增编辑按钮，可通过弹窗修改昵称。
4. 昵称更新后立即刷新当前用户状态和账号栏展示。
5. 集成测试覆盖昵称更新、再次读取当前用户，以及搜索结果作者名随昵称更新。

验证：

```bash
python3 -m py_compile backend/src/noteweave/api/schemas.py backend/src/noteweave/api/app.py backend/tests/test_mvp_flow.py
node --check frontend/app.js
node frontend/tree.test.js
node frontend/render.test.js
cd /root/project/NoteWeave/backend
timeout 30s env UV_CACHE_DIR=/root/project/NoteWeave/.uv-cache ../.tools/uv/bin/uv run --no-sync pytest -vv -s
```

结果：

```text
frontend tree/render tests passed
1 passed in 2.25s
```

遗留：
1. 个人资料目前只支持修改昵称，不支持头像、密码修改或系统角色申请。
2. 前端编辑仍使用轻量 `prompt`，后续可统一替换为应用内弹窗。

下一步：
1. 提交个人资料编辑检查点。
2. 继续补 AI 配置状态页、导出或评论分页能力。

## 2026-06-02 AI 配置状态切片

目标：
补齐 AI 模块“配置接口状态”的展示能力，让用户能确认当前是远程模型还是本地降级，同时不暴露 API Key。

完成：
1. 新增 `GET /api/ai/config/status`，登录用户可查看 AI 配置状态。
2. 接口返回是否启用 AI、远程模型是否完整配置、模型名是否配置、Base URL/API Key 是否配置、本地降级是否可用。
3. 接口只返回 `api_key_configured` 布尔值，不返回密钥原文。
4. 设置页新增“AI 配置状态”面板，显示启用状态、远程模型配置状态、模型名和本地降级状态。
5. 前端登录后自动加载 AI 状态，并在设置页渲染。
6. 集成测试覆盖 AI 配置状态接口和密钥不泄露。

验证：

```bash
python3 -m py_compile backend/src/noteweave/api/app.py backend/tests/test_mvp_flow.py
node --check frontend/app.js
node frontend/tree.test.js
node frontend/render.test.js
cd /root/project/NoteWeave/backend
timeout 30s env UV_CACHE_DIR=/root/project/NoteWeave/.uv-cache ../.tools/uv/bin/uv run --no-sync pytest -vv -s
```

结果：

```text
frontend tree/render tests passed
1 passed in 2.02s
```

遗留：
1. AI 配置仍通过环境变量提供，前端只读状态，不在页面内修改配置。
2. 目前没有真实远程模型连通性探测，只判断配置项是否齐全。

下一步：
1. 提交 AI 配置状态检查点。
2. 继续补导出、评论分页或用户密码修改能力。

## 2026-06-02 评论分页切片

目标：
补齐评论列表的分页读取能力，避免笔记/错题/建议讨论增长后只能随详情一次性加载。

完成：
1. 新增 `GET /api/comments`，支持按 `target_type`、`target_id`、`limit`、`offset` 分页读取评论。
2. 评论读取和创建都会校验目标内容访问权限；私有笔记、私有错题不会被同课程的其他成员通过评论接口访问。
3. 前端笔记卡片新增“查看评论”入口，按分页接口读取前 20 条评论并展示。
4. 集成测试覆盖评论分页结果和私有笔记评论访问控制。

验证：

```bash
python3 -m py_compile backend/src/noteweave/api/app.py backend/tests/test_mvp_flow.py
node --check frontend/app.js
node frontend/tree.test.js
node frontend/render.test.js
cd /root/project/NoteWeave/backend
timeout 30s env UV_CACHE_DIR=/root/project/NoteWeave/.uv-cache ../.tools/uv/bin/uv run --no-sync pytest -vv -s
```

结果：
```text
frontend tree/render tests passed
1 passed in 2.23s
```

遗留：
1. 前端评论展示仍是轻量 `alert`，还没有做成完整讨论面板。
2. 错题和建议的评论查看入口暂未放到前端对应列表。

下一步：
1. 提交评论分页检查点。
2. 继续补用户密码修改或导出能力。

## 2026-06-02 密码修改切片

目标：
补齐用户在个人设置中修改密码的基本能力，和已完成的昵称编辑放在同一用户资料接口内。

完成：
1. `PATCH /api/auth/me` 支持可选 `current_password`、`new_password` 字段。
2. 修改密码时校验当前密码，新密码沿用注册密码的长度约束。
3. 前端账号栏新增“修改密码”按钮，使用轻量提示框完成当前密码和新密码输入。
4. 集成测试覆盖错误当前密码、旧密码失效和新密码可登录。

验证：
```bash
python3 -m py_compile backend/src/noteweave/api/app.py backend/src/noteweave/api/schemas.py backend/tests/test_mvp_flow.py
node --check frontend/app.js
node frontend/tree.test.js
node frontend/render.test.js
cd /root/project/NoteWeave/backend
timeout 30s env UV_CACHE_DIR=/root/project/NoteWeave/.uv-cache ../.tools/uv/bin/uv run --no-sync pytest -vv -s
```

结果：
```text
frontend tree/render tests passed
1 passed in 2.36s
```

遗留：
1. 修改密码后不会自动注销其他已签发 token。
2. 前端仍使用 `prompt` 输入密码，后续可改成应用内表单弹窗。

下一步：
1. 提交密码修改检查点。
2. 继续补版本记录展示或导出能力。

## 2026-06-02 版本记录展示切片

目标：
补齐笔记关键修改记录的可演示入口，让用户能从笔记卡片查看历史版本快照。

完成：
1. 前端笔记卡片新增“版本”操作，调用 `GET /api/notes/{note_id}/versions`。
2. 版本弹窗展示版本 ID、创建时间、修改人、变更原因和快照标题。
3. 集成测试覆盖初始版本、手动更新版本、发布版本的顺序和快照正文。

验证：
```bash
python3 -m py_compile backend/src/noteweave/api/app.py backend/tests/test_mvp_flow.py
node --check frontend/app.js
node frontend/tree.test.js
node frontend/render.test.js
cd /root/project/NoteWeave/backend
timeout 30s env UV_CACHE_DIR=/root/project/NoteWeave/.uv-cache ../.tools/uv/bin/uv run --no-sync pytest -vv -s
```

结果：
```text
frontend tree/render tests passed
1 passed in 2.13s
```

遗留：
1. 版本展示仍是轻量 `alert`，没有做差异对比或恢复版本。
2. 版本记录目前只覆盖笔记，不覆盖错题或知识树节点。

下一步：
1. 提交版本记录展示检查点。
2. 继续补思维导图、导出或更完整的讨论面板。

## 2026-06-02 轻量思维导图切片

目标：
在不引入重依赖的前提下，补齐笔记和错题中插入、保存、预览思维导图内容的基础能力。

完成：
1. Markdown 渲染器支持 `mindmap` 代码块，并将缩进层级渲染为嵌套导图节点。
2. 兼容 `mermaid` 代码块中以 `mindmap` 开头的内容，便于后续接入 Mermaid。
3. 笔记和错题编辑区新增“插入思维导图”按钮，自动写入可编辑模板。
4. 前端渲染测试覆盖思维导图代码块。

验证：
```bash
node --check frontend/app.js
node frontend/render.test.js
node frontend/tree.test.js
python3 -m py_compile backend/src/noteweave/api/app.py backend/tests/test_mvp_flow.py
cd /root/project/NoteWeave/backend
timeout 30s env UV_CACHE_DIR=/root/project/NoteWeave/.uv-cache ../.tools/uv/bin/uv run --no-sync pytest -vv -s
```

结果：
```text
frontend tree/render tests passed
1 passed in 2.01s
```

遗留：
1. 当前是轻量静态导图预览，不支持拖拽编辑、缩放或节点折叠。
2. 未接入 Mermaid 或专业思维导图库，后续可在不改存储格式的基础上替换渲染层。

下一步：
1. 提交轻量思维导图检查点。
2. 继续补课程搜索加入或导出能力。

## 2026-06-02 课程搜索加入切片

目标：
补齐通过课程搜索发现并加入课程的 P1 能力，作为邀请码加入之外的轻量入口。

完成：
1. 新增 `GET /api/courses/search`，支持按课程名称、简介、学期和标签搜索。
2. 搜索结果不暴露邀请码，只返回公开展示字段、成员数和当前用户是否已加入。
3. 新增 `POST /api/courses/{course_id}/join-public`，允许从搜索结果直接加入课程。
4. 前端侧边栏新增课程搜索框、搜索结果列表和“加入课程”按钮。
5. 集成测试覆盖未加入用户搜索课程、搜索结果不泄露邀请码、按搜索结果加入课程。

验证：
```bash
python3 -m py_compile backend/src/noteweave/api/app.py backend/tests/test_mvp_flow.py
node --check frontend/app.js
node frontend/tree.test.js
node frontend/render.test.js
cd /root/project/NoteWeave/backend
timeout 30s env UV_CACHE_DIR=/root/project/NoteWeave/.uv-cache ../.tools/uv/bin/uv run --no-sync pytest -vv -s
```

结果：
```text
frontend tree/render tests passed
1 passed in 2.49s
```

遗留：
1. 目前所有课程都可被搜索并直接加入；后续如需要严格班级准入，可增加课程公开性、申请审批或邀请码必填策略。
2. 搜索结果是侧边栏轻量列表，尚未做独立课程发现页。

## 2026-06-02 前端体验优化与 Landing 页面切片

目标：
1. 确认当前前端是否已使用 React/Vue，并补齐高端简洁风格的展示页与主工作台视觉升级。

完成：
1. 确认当前前端为纯前端静态实现（无 React/Vue、无构建链）。
2. 新增 `frontend/landing.html`，独立展示产品定位、核心能力、流程和 CTA。
3. 新增 `frontend/landing.css`，配套现代化信息架构页面样式。
4. 优化 `frontend/styles.css` 视觉：统一色板、卡片化面板、增强按钮/表单/列表交互状态，降低“简陋感”。
5. 工作台右上角新增“产品介绍”入口，支持从应用快速跳回 landing。
6. 更新 `README.md`，说明 landing 与工作台访问入口。

验证：
```bash
node --check frontend/app.js
node --check frontend/render.js
node --check frontend/tree.js
node --check frontend/tree.test.js
node frontend/render.test.js
python3 -m py_compile backend/src/noteweave/api/app.py backend/tests/test_mvp_flow.py
```

结果：
```text
前端语法检查通过；后端现有用例仍通过（当前项目仍保持原有测试通过状态）。
```

遗留：
1. 评论面板、讨论抽屉、详情级布局仍待下一阶段重构。
2. 如需真正达到组件级协作体验，可后续逐步迁移到 React/Vue 实现。

下一步：
1. 提交前端体验优化切片。
2. 继续优化评论与讨论入口，补齐错题评论入口和分页加载。

## 2026-06-02 Vue 单页路由切片

目标：
把前端从“独立 landing/workspace html 模板”切到 Vue 路由驱动的 SPA 方式，避免内容继续在原生 HTML 页面内静态维护。

完成：
1. 重写 `frontend/vue-bootstrap.js`，改为基于 `window.location.hash` 的前端 route（`#landing` / `#workspace`）。
2. 模板内导航链接改为 `data-route` + 统一事件分发，点击按钮切换页面时不离开单页入口。
3. `index.html` 入口改为纯 Vue 壳，默认从路由启动，不再依赖 `data-page` 直接写死模板。
4. 更新 `README.md` 页面入口说明，明确 SPA 与 `#workspace` 的访问方式。

验证：
```bash
node --check frontend/page-templates.js
node --check frontend/vue-bootstrap.js
```

结果：
```
前端语法检查通过
```

遗留：
1. 尚未引入构建工具链，继续沿用 CDN Vue 与模板字符串渲染策略，后续如需组件化可拆分为单独 Vue 组件文件。

## 2026-06-02 入口收敛切片

目标：
去掉独立 `landing.html` / `workspace.html` 重定向入口，收敛为 `index.html` 单一 Vue SPA 入口。

完成：
1. 删除 `frontend/landing.html` 与 `frontend/workspace.html`。
2. 统一以 `index.html#landing` 与 `index.html#workspace` 的路由入口承载完整页面。
3. 保留页面内 `data-route` 导航策略，跳转始终不离开工作台。

验证：
```bash
node --check frontend/vue-bootstrap.js
node --check frontend/page-templates.js
node --check frontend/app.js
node --check frontend/render.js
node --check frontend/tree.js
```

结果：
```
前端语法检查通过
```

遗留：
1. 兼容跳转页面移除后，团队内部文档和运维说明需同步复核，避免残留旧链接。

## 2026-06-02 树结构移动安全与回归验证

目标：
补齐知识树移动操作的完整性检查，避免将节点移动到自身子树导致的环形结构。

完成：
1. 后端 `POST /api/tree/nodes/{node_id}/move` 增加循环检测：禁止把节点移动到其子树内（包括自身）。
2. 新增数据库辅助查询 `is_descendant(...)`，用于后续树关系校验复用。
3. 新增后端测试 `test_node_move_rejects_cyclic_hierarchy`，覆盖：
   - 尝试把父节点移到孙子节点下应返回 400。
   - 将子节点移回根节点应成功。
4. 本轮同步了后端回归与前端静态检查。

验证：
```bash
cd /root/project/NoteWeave/backend
UV_CACHE_DIR=/root/project/NoteWeave/.uv-cache ../.tools/uv/bin/uv run --no-sync pytest -q
cd /root/project/NoteWeave
node --check frontend/app.js frontend/vue-bootstrap.js frontend/page-templates.js
cd /root/project/NoteWeave/frontend
node tree.test.js
node render.test.js
```

结果：
```text
4 passed in 3.21s
```

遗留：
1. 前端树编辑尚未阻止用户发起非法移动动作；当前仅依赖接口返回错误。

## 2026-06-02 前端 Vite 重建切片

目标：
按用户要求废弃当前无构建链前端，把现有 `frontend/` 整体改名为 `frontend-old/`，重新创建 Vue 3 + Vite + TypeScript 前端工程。

完成：
1. 将旧前端整体移动到 `frontend-old/`，保留历史实现、测试和样式作为迁移参考。
2. 新建 `frontend/`，包含 `package.json`、`vite.config.ts`、`tsconfig.json`、`src/` 和 Vite 入口。
3. 使用 Vue SFC 重建 Landing 页面、课程工作台、知识树组件和 API service。
4. 前端依赖使用 `vue`、`@lucide/vue`、`vite`、`typescript`、`vue-tsc`。
5. 更新 README、架构文档和项目规范，明确新前端使用 Vue 3 + Vite + TypeScript。

验证：
```bash
cd /root/project/NoteWeave/frontend
npm install
npm run typecheck
npm run build
```

结果：
```text
npm run typecheck 通过
npm run build 通过，Vite 成功生成 dist/
```

遗留：
1. 新前端已接入登录、课程、知识树、笔记、错题和检索的基础 API，但旧前端中的评论、收藏、点赞、AI 结果采纳、成员管理等高级交互还需要逐步迁移。
2. 旧前端测试位于 `frontend-old/`，新前端后续需要补 Vitest/组件测试或 Playwright smoke。

下一步：
1. 启动开发服务器检查页面实际渲染，再继续迁移旧前端的高级协作功能。

## 2026-06-02 演示环境部署

目标：
将当前 demo 部署到 `wu-uk.com/noteweave/`。

完成：
1. 执行 `npm run build:noteweave`，生成带 `/noteweave/` base path 的前端产物。
2. 将 `frontend/dist/` 同步到 `/var/www/wu-uk.com/html/noteweave/`。
3. 新建并启用 `noteweave-backend.service`，后端监听 `127.0.0.1:8000`。
4. 配置 Nginx：
   - `/noteweave/` 指向前端静态文件。
   - `/noteweave/api/` 反代到 FastAPI `/api/`。
   - `/noteweave/health` 反代到 FastAPI `/health`。

验证：
```bash
systemctl status noteweave-backend --no-pager
curl -sS http://127.0.0.1:8000/health
curl -I http://wu-uk.com/noteweave/
curl -sS http://wu-uk.com/noteweave/health
```

结果：
```text
noteweave-backend active (running)
http://127.0.0.1:8000/health -> {"status":"ok"}
http://wu-uk.com/noteweave/ -> 200 OK
http://wu-uk.com/noteweave/health -> {"status":"ok"}
```

遗留：
1. systemd 服务当前未配置远程模型 API 环境变量，AI 状态会显示模型未配置；基础 demo 功能可用。
2. HTTPS 仍取决于 Cloudflare Edge Certificate 状态，暂不强制 HTTP 跳 HTTPS。

## 2026-06-02 新 Vue 前端协作互动迁移

目标：
继续从 `frontend-old/` 迁移协作闭环能力到 Vue 3 + Vite + TypeScript 新前端，优先覆盖需求文档中的共享互动 P0 功能。

完成：
1. 前端类型新增评论、互动状态字段：
   - `Note.is_liked`、`like_reaction_id`、`is_favorite`、`favorite_reaction_id`、`comment_count`。
   - `Mistake.is_favorite`、`favorite_reaction_id`。
   - `Comment` 类型。
2. `frontend/src/services/api.ts` 新增：
   - 发布笔记。
   - 点赞/收藏创建与取消。
   - 评论查询与发布。
   - 错题掌握状态更新。
3. `WorkspacePage.vue` 迁移互动 UI：
   - 笔记卡片支持发布、点赞/取消点赞、收藏/取消收藏、打开讨论。
   - 错题卡片支持掌握状态切换、收藏/取消收藏、打开讨论。
   - 新增评论抽屉，可查看和发布笔记/错题评论。
4. 更新样式，补齐互动按钮、选中态、摘要提示和讨论抽屉。
5. 重新构建并同步部署到 `wu-uk.com/noteweave/`。

验证：
```bash
cd /root/project/NoteWeave/frontend
npm run typecheck
npm run build:noteweave
rsync -a --delete /root/project/NoteWeave/frontend/dist/ /var/www/wu-uk.com/html/noteweave/
curl -I http://wu-uk.com/noteweave/
curl -sS http://wu-uk.com/noteweave/health
systemctl status noteweave-backend --no-pager
```

结果：
```text
npm run typecheck 通过
npm run build:noteweave 通过
http://wu-uk.com/noteweave/ -> 200 OK
http://wu-uk.com/noteweave/health -> {"status":"ok"}
noteweave-backend active (running)
```

遗留：
1. 补充/纠错建议还没有迁移到新 Vue 前端。
2. AI 摘要/标签生成与采纳、成员管理、审计日志、笔记版本仍需继续迁移。

## 2026-06-02 新 Vue 前端补充/纠错建议迁移

目标：
继续迁移需求文档中的协作闭环能力，把补充、纠错建议和处理状态接入 Vue 3 + Vite + TypeScript 新前端。

完成：
1. 前端新增 `Suggestion`、`SuggestionStatus` 类型。
2. `frontend/src/services/api.ts` 新增建议相关接口：
   - `listSuggestions`
   - `createSuggestion`
   - `handleSuggestion`
3. `WorkspacePage.vue` 迁移建议 UI：
   - 笔记卡片新增“补充”“纠错”入口。
   - 错题卡片新增“纠错”入口。
   - 右侧知识点上下文新增“建议当前知识点”入口。
   - 右侧面板新增建议提交表单。
   - 右侧面板新增建议列表和待处理建议状态操作。
4. 样式新增建议表单、建议列表、紧凑操作按钮和危险操作弱提示。
5. 重新构建并同步部署到 `wu-uk.com/noteweave/`。

验证：
```bash
cd /root/project/NoteWeave/frontend
npm run typecheck
npm run build:noteweave
rsync -a --delete /root/project/NoteWeave/frontend/dist/ /var/www/wu-uk.com/html/noteweave/
curl -I http://wu-uk.com/noteweave/
curl -sS http://wu-uk.com/noteweave/health
systemctl status noteweave-backend --no-pager
```

结果：
```text
npm run typecheck 通过
npm run build:noteweave 通过
http://wu-uk.com/noteweave/ -> 200 OK
http://wu-uk.com/noteweave/health -> {"status":"ok"}
noteweave-backend active (running)
```

遗留：
1. 处理建议接口需要课程维护者权限；普通学生点击处理会收到后端权限错误。
2. AI 摘要/标签生成与采纳、成员管理、审计日志、笔记版本仍需继续迁移。

## 2026-06-02 新 Vue 前端 AI 摘要/标签迁移

目标：
迁移需求文档中的 AI 摘要、关键词标签提取和人工采纳流程，让新 Vue 前端支持 AI 结果预览、编辑、采纳或放弃。

完成：
1. 前端新增 `AiTaskType` 和 `AiResult` 类型。
2. `frontend/src/services/api.ts` 新增：
   - `generateNoteAi`
   - `acceptAiResult`
   - `rejectAiResult`
3. 笔记卡片新增“摘要”和“标签”操作。
4. 新增 AI 结果抽屉：
   - 摘要任务展示可编辑摘要文本。
   - 标签任务展示可编辑逗号分隔标签。
   - 支持采纳和放弃。
   - 显示结果来源和错误提示；远程模型未配置时后端 fallback 结果仍可使用。
5. 重新构建并同步部署到 `wu-uk.com/noteweave/`。

验证：
```bash
cd /root/project/NoteWeave/frontend
npm run typecheck
npm run build:noteweave
rsync -a --delete /root/project/NoteWeave/frontend/dist/ /var/www/wu-uk.com/html/noteweave/
curl -I http://wu-uk.com/noteweave/
curl -sS http://wu-uk.com/noteweave/health
systemctl status noteweave-backend --no-pager
```

结果：
```text
npm run typecheck 通过
npm run build:noteweave 通过
http://wu-uk.com/noteweave/ -> 200 OK
http://wu-uk.com/noteweave/health -> {"status":"ok"}
noteweave-backend active (running)
```

遗留：
1. systemd 仍未配置远程模型 API 环境变量，当前 AI 主要依赖后端 fallback；基础流程可演示。
2. 成员管理、审计日志、笔记版本仍需继续迁移。

## 2026-06-02 新 Vue 前端成员管理与审计日志迁移

目标：
迁移需求文档中的课程成员角色管理和架构文档中的关键操作审计展示能力。

完成：
1. 前端新增 `CourseRole`、`CourseMember`、`AuditLog` 类型。
2. `frontend/src/services/api.ts` 新增：
   - `updateCourse`
   - `listMembers`
   - `updateMemberRole`
   - `removeMember`
   - `listAuditLogs`
3. 右侧面板新增“课程管理”区域：
   - 编辑课程名称、学期、说明、标签。
   - 查看课程成员。
   - 调整成员角色。
   - 移除成员。
   - 查看审计日志。
4. 课程切换时自动加载成员列表和审计日志；普通成员无权读取审计日志时显示空态，不阻塞主流程。
5. 重新构建并同步部署到 `wu-uk.com/noteweave/`。

验证：
```bash
cd /root/project/NoteWeave/frontend
npm run typecheck
npm run build:noteweave
rsync -a --delete /root/project/NoteWeave/frontend/dist/ /var/www/wu-uk.com/html/noteweave/
curl -I http://wu-uk.com/noteweave/
curl -sS http://wu-uk.com/noteweave/health
systemctl status noteweave-backend --no-pager
```

结果：
```text
npm run typecheck 通过
npm run build:noteweave 通过
http://wu-uk.com/noteweave/ -> 200 OK
http://wu-uk.com/noteweave/health -> {"status":"ok"}
noteweave-backend active (running)
```

遗留：
1. 成员角色调整和移除需要课程维护者权限；普通学生操作会收到后端权限错误。
2. 笔记版本仍需继续迁移。

## 2026-06-02 新 Vue 前端笔记版本迁移

目标：
迁移需求文档中的笔记版本记录查看能力，补齐新版 Vue 工作台的版本追踪入口。

完成：
1. 前端新增 `NoteVersion` 类型。
2. `frontend/src/services/api.ts` 新增 `listNoteVersions`。
3. 笔记卡片新增“版本”操作。
4. 新增版本记录抽屉，展示版本 ID、标题、变更时间、修改人、变更原因和正文快照。
5. 新增版本列表样式。
6. 重新构建并同步部署到 `wu-uk.com/noteweave/`。

验证：
```bash
cd /root/project/NoteWeave/frontend
npm run typecheck
npm run build:noteweave
rsync -a --delete /root/project/NoteWeave/frontend/dist/ /var/www/wu-uk.com/html/noteweave/
curl -I http://wu-uk.com/noteweave/
curl -sS http://wu-uk.com/noteweave/health
systemctl status noteweave-backend --no-pager
```

结果：
```text
npm run typecheck 通过
npm run build:noteweave 通过
http://wu-uk.com/noteweave/ -> 200 OK
http://wu-uk.com/noteweave/health -> {"status":"ok"}
noteweave-backend active (running)
```

遗留：
1. 当前版本抽屉只读快照，不支持版本 diff 或一键恢复。

## 2026-06-02 新 Vue 前端笔记/错题编辑删除迁移

目标：
补齐需求文档中笔记和错题的基础 CRUD 闭环，让新 Vue 工作台不仅能创建，也能编辑和删除已有内容。

完成：
1. `frontend/src/services/api.ts` 新增：
   - `updateNote`
   - `deleteNote`
   - `updateMistake`
   - `deleteMistake`
2. 笔记表单支持创建/编辑复用：
   - 点击笔记卡片“编辑”后回填标题、正文、标签、可见性。
   - 提交时根据 `editingId` 创建或更新。
   - 支持取消编辑。
3. 笔记卡片新增“编辑”“删除”操作。
4. 错题表单支持创建/编辑复用：
   - 点击错题卡片“编辑”后回填题目、答案、标签、掌握状态。
   - 提交时根据 `editingId` 创建或更新。
   - 支持取消编辑。
5. 错题卡片新增“编辑”“删除”操作。
6. 删除后自动刷新列表、知识树统计和建议列表。
7. 重新构建并同步部署到 `wu-uk.com/noteweave/`。

验证：
```bash
cd /root/project/NoteWeave/frontend
npm run typecheck
npm run build:noteweave
rsync -a --delete /root/project/NoteWeave/frontend/dist/ /var/www/wu-uk.com/html/noteweave/
curl -I http://wu-uk.com/noteweave/
curl -sS http://wu-uk.com/noteweave/health
systemctl status noteweave-backend --no-pager
```

结果：
```text
npm run typecheck 通过
npm run build:noteweave 通过
http://wu-uk.com/noteweave/ -> 200 OK
http://wu-uk.com/noteweave/health -> {"status":"ok"}
noteweave-backend active (running)
```

遗留：
1. 当前错题编辑只覆盖新前端表单已有字段：题目、正确答案、标签、掌握状态；错因、解析、反思等扩展字段后续可继续补齐。
2. 删除操作当前直接执行，后续可补应用内确认弹窗。

## 2026-06-02 笔记核心方向重构

目标：
根据最新产品判断，把 NoteWeave 从“课程协作工作台”调整为“笔记核心 Demo”。前端不再暴露新建课程、加入课程和课程角色管理；普通用户添加笔记后，由 AI 自动判断课程分类和知识点，并借鉴 MoDora 的解析/检索/问答流程。

完成：
1. 安装 Anthropic `frontend-design` skill，并按“精致、克制、笔记软件感”的方向重写前端体验。
2. `docs/需求文档.md` 重写为笔记优先需求：
   - 只保留普通用户和管理员。
   - 取消手动建课/加入课程/课程成员角色。
   - 增加 AI 自动课程归类和笔记问答要求。
3. `docs/架构文档.md` 重写为新架构：
   - 课程作为 AI 自动分类容器。
   - 主链路为 `notes/ingest` 和 `notes/ask`。
   - 明确只借鉴 MoDora 的解析、结构化、检索、问答流程，不引入重依赖。
4. 后端新增：
   - `POST /api/notes/ingest`
   - `POST /api/notes/ask`
   - `AIAssistService.classify_note`
   - `AIAssistService.answer_question`
   - fallback 分类、摘要、标签和问答。
5. 前端新增：
   - 新 landing 页面，介绍笔记摄取、AI 自动归类、共享互动和笔记问答。
   - 新工作台三栏布局：AI 分类、笔记流、解析/问答/当前笔记。
   - 移除主界面中新建课程、加入课程、成员角色管理入口。
6. 保留旧课程 API 作为兼容层，避免破坏已有数据和测试。

验证：
```bash
cd /root/project/NoteWeave/backend
env UV_CACHE_DIR=/root/project/NoteWeave/.uv-cache ../.tools/uv/bin/uv run pytest

cd /root/project/NoteWeave/frontend
npm run typecheck
npm run build:noteweave
```

结果：
```text
backend pytest 通过：4 passed
frontend typecheck 通过
frontend build:noteweave 通过
```

遗留：
1. 当前“上传笔记”还主要是文本摄取，附件上传入口需要接回新工作台。
2. 管理员页面尚未实现。
3. 旧课程 API 和旧测试仍保留，后续应逐步把课程语义收缩为内部分类服务。
