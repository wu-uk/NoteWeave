# NoteWeave 开发日志

## 2026-06-02

目标：
建立项目开发规范，并完成后端 MVP 首个可运行切片。

完成：
1. 新增 `docs/项目规范文档.md`，约定长期 goal 模式、git 检查点、uv 环境、开发日志和验证方式。
2. 安装 `python3.14-venv`，在项目内创建 `.tools/uv`，并安装 `uv 0.11.17`。
3. 新增 `.gitignore`，排除 `.venv/`、`.tools/`、`.uv-cache/`、运行数据库、缓存和前端构建产物。
4. 新增 `backend/pyproject.toml`，后端使用 uv 管理依赖。
5. 新增 `backend/src/noteweave/` 后端包，完成 FastAPI + SQLite 轻量后端。
6. 首版接口覆盖注册登录、课程、成员、知识树、笔记、版本、错题、评论、点赞/收藏、补充/纠错、关键词检索、AI 摘要和标签建议。
7. AI 当前是本地规则降级实现，不依赖真实模型 API；后续可替换为远程 OpenAI-compatible client。
8. 新增 `backend/tests/test_mvp_flow.py`，覆盖课程笔记协作核心闭环。

验证：

```bash
cd /root/project/NoteWeave/backend
env UV_CACHE_DIR=/root/project/NoteWeave/.uv-cache ../.tools/uv/bin/uv run --no-sync pytest
```

结果：

```text
1 passed in 1.32s
```

注意：
1. 当前环境中 `uv` 不在系统 PATH，使用项目内 `../.tools/uv/bin/uv`。
2. uv 默认缓存目录 `/root/.cache/uv` 当前不可写，命令需要设置 `UV_CACHE_DIR=/root/project/NoteWeave/.uv-cache`。
3. `uv run pytest` 会尝试同步依赖并联网；依赖已安装时使用 `uv run --no-sync pytest`。
4. 当前 FastAPI/Starlette 在 Python 3.14 环境中同步路由/同步依赖会挂起，因此后端 API 路由和 FastAPI 依赖函数均使用 `async def`。

遗留：
1. 还没有前端应用。
2. 后端暂用标准库 SQLite repository，后续可按架构文档演进到 SQLAlchemy/SQLModel。
3. AI 模块还没有接入远程模型 API。
4. 文件上传、图片附件和富文本编辑器尚未实现。
5. 当前测试只有一条端到端 MVP flow，后续需要拆分更多权限和异常场景测试。

下一步：
1. 提交后端首个开发检查点。
2. 补充根 README 的运行说明。
3. 继续实现前端课程工作台或完善后端文件/附件能力。

## 2026-06-02 前端工作台切片

目标：
补上可演示的课程工作台前端，连接后端 MVP API。

完成：
1. 新增 `frontend/index.html`、`frontend/styles.css`、`frontend/app.js`。
2. 前端覆盖登录/注册、课程创建/加入、知识树浏览和节点创建、笔记创建/发布/点赞/评论/AI 摘要/AI 标签、错题录入和复习状态、关键词检索、补充/纠错建议。
3. 后端新增 `POST /api/courses/join`，支持只用邀请码加入课程。
4. 更新 README，加入前端打开方式和静态服务预览命令。

验证：

```bash
node --check frontend/app.js
python3 -m py_compile backend/src/noteweave/api/app.py backend/tests/test_mvp_flow.py
cd /root/project/NoteWeave/backend
timeout 30s env UV_CACHE_DIR=/root/project/NoteWeave/.uv-cache ../.tools/uv/bin/uv run --no-sync pytest
cd /root/project/NoteWeave/frontend
python3 -m http.server 5173 --bind 127.0.0.1
curl -I http://127.0.0.1:5173/
```

结果：

```text
1 passed in 1.23s
frontend static server: HTTP/1.0 200 OK
```

遗留：
1. 前端仍是无构建静态应用，还没有 Vue/React 组件化。
2. 还没有真实富文本编辑器、图片附件、思维导图。
3. 协作建议列表目前只保留本次会话提交结果，后端还需要补建议查询接口。
4. 还没有浏览器截图自动化测试。

下一步：
1. 提交前端工作台检查点。
2. 补充后端建议查询、附件上传或引入组件化前端。

## 2026-06-02 协作和 AI 接口整理切片

目标：
补齐协作建议的持久查询闭环，并将 AI 功能整理为可配置远程 API、缺省降级的结构。

完成：
1. 新增 `backend/src/noteweave/core/ai.py`，提供 `AIAssistService`。
2. AI 摘要和标签接口现在优先使用 `NOTEWEAVE_MODEL_BASE_URL`、`NOTEWEAVE_MODEL_API_KEY`、`NOTEWEAVE_CHAT_MODEL` 配置的 OpenAI-compatible `/chat/completions` API。
3. 远程模型未配置、关闭或调用失败时，AI 接口自动使用本地规则降级，并在结果中标记 `source: fallback`。
4. 新增 `GET /api/courses/{course_id}/suggestions`，课程成员可查询课程内补充/纠错建议，支持按状态过滤。
5. 前端协作建议列表改为从后端加载，处理建议后自动刷新。
6. `httpx` 从 dev 依赖调整为后端运行依赖，并更新 `backend/uv.lock`。

验证：

```bash
python3 -m py_compile backend/src/noteweave/api/app.py backend/src/noteweave/core/ai.py backend/tests/test_mvp_flow.py
node --check frontend/app.js
cd /root/project/NoteWeave/backend
timeout 30s env UV_CACHE_DIR=/root/project/NoteWeave/.uv-cache ../.tools/uv/bin/uv run --no-sync pytest
```

结果：

```text
1 passed in 1.34s
```

遗留：
1. 远程 AI 目前按 OpenAI-compatible chat completions 调用，尚未针对具体供应商做错误码和限流处理。
2. 还没有前端 AI 配置页面；配置仍通过环境变量完成。
3. 附件上传和真实富文本编辑器仍未实现。

下一步：
1. 提交协作和 AI 整理检查点。
2. 继续做附件/图片能力或前端组件化。
